import type { NumberToStringType } from "./toString.type";

/**
 * Number to string.
 *
 * @param number Number to analyze.
 * @returns String.
 */
export const numberToString: NumberToStringType = (number) => {
  return String(number);
};
